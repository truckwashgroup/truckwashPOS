node_modules
dist
release
.DS_Store
*.local

# Native builds (de projectmappen android/ en ios/ horen er WEL in)
android/.gradle
android/app/build
# De webbundel en de config in het Android-project worden door `cap sync`
# gemaakt. Ze meecommitten betekent een diff van duizenden regels bij elke
# build, en een kopie van de app die kan gaan afwijken van src/.
android/app/src/main/assets/public
android/app/src/main/assets/capacitor.config.json
android/build
android/local.properties
android/.idea
android/app/release
ios/App/Pods
ios/App/build
ios/App/App/public
ios/DerivedData
*.xcuserstate

# Ondertekening -- nooit in git
*.keystore
*.jks
*.p12
*.mobileprovision

# gebouwde APK in de projectmap
Truckwash1-Kassa-*.apk

# geheimen
.env
.env.local
.env.*.local

# Werkbestanden van de Supabase-CLI
supabase/.temp/

# Schermafdrukken zijn werkmateriaal, geen broncode.
schermafdrukken/
